function Header(){
    return (
        <>
        <h1>This is a Heading</h1>
        </>
    )
}
export default Header;  
// * these are state less component
// * as we are using default we can import this function component to any name
// * Differcence between export and export default --> by doing only export,
// you need to import with same name inside {} 
// * <></> -> this is called React.Fragment 